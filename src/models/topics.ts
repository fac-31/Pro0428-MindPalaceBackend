
import { Request, Response } from "express";



export async function getTopicsModel() {
    console.log("entered model");
    try {

    
    }
    catch (error) {
        console.log(error.error);
        throw error;
    }
}
